# Must match FRONTEND_SECRET in backend/.env (used for AES level-1)
VITE_FRONTEND_SECRET=replace-with-strong-random-string

# Backend API base URL (no trailing slash)
VITE_API_URL=http://localhost:8100/api
